name: CI/CD tr-dss-slopes-data

on: 
  push:
    paths:
      - "transformers/dss-slopes-data/infrastructure/**"
      - "transformers/dss-slopes-data/src/**"
      - ".github/workflows/tr-dss-slopes-data.yml"

env:
  WORKING_DIRECTORY: transformers/dss-slopes-data
  DOCKER_IMAGE: ghcr.io/noi-techpark/opendatahub-collectors/tr-dss-slopes
  DOCKER_TAG: ${{ github.sha }}
  KUBERNETES_NAMESPACE: collector

jobs:
  test:
    runs-on: ubuntu-24.04
    concurrency: tr-dss-slopes-data-test
    steps:
    - name: Checkout source code
      uses: actions/checkout@v4

    - name: Run tests
      run: docker run --rm $(docker build -q . -f infrastructure/docker/Dockerfile --target test)
      working-directory: ${{ env.WORKING_DIRECTORY }}

  build:
    runs-on: ubuntu-24.04
    concurrency: tr-dss-slopes-data-build
    needs:
      - test
    steps:
    - name: Checkout source code
      uses: actions/checkout@v4

    - name: Build and push images
      uses: noi-techpark/github-actions/docker-build-and-push@v2
      with:
        working-directory: ${{ env.WORKING_DIRECTORY }}/infrastructure
        docker-username: ${{ github.actor }}
        docker-password: ${{ secrets.GITHUB_TOKEN }}

  deploy-test:
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/feat/superskidata'
    needs:
      - build
    runs-on: ubuntu-24.04
    concurrency: tr-dss-slopes-data-deploy-test
    environment: test
    env:
      PROJECT_NAME: tr-dss-slopes
      VALUES_YAML: infrastructure/helm/slopes-data.test.yaml
    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Customize values.yaml
        working-directory: ${{ env.WORKING_DIRECTORY }}
        run: |
          yq -i '
            .image.repository="${{ env.DOCKER_IMAGE }}" |
            .image.tag="${{ env.DOCKER_TAG }}" |
            .image.pullPolicy="IfNotPresent"
          ' ${{ env.VALUES_YAML }}

          yq -i '.envSecret.ODH_CORE_TOKEN_CLIENT_ID = "${{ secrets.CONTENT_DSS_CLIENT_ID }}"' ${{ env.VALUES_YAML }}
          yq -i '.envSecret.ODH_CORE_TOKEN_CLIENT_SECRET = "${{ secrets.CONTENT_DSS_CLIENT_SECRET }}"' ${{ env.VALUES_YAML }}

      - name: Deploy on cluster  
        uses: noi-techpark/github-actions/helm-deploy@v2
        with:
          k8s-name: ${{ env.PROJECT_NAME }}
          k8s-namespace: collector
          chart-path: helm/generic-collector
          values-file: ${{ env.WORKING_DIRECTORY }}/${{ env.VALUES_YAML }}
          aws-access-key-id: ${{ secrets[vars.AWS_KEY_ID] }}
          aws-secret-access-key: ${{ secrets[vars.AWS_KEY_SECRET] }}
          aws-eks-cluster-name: aws-main-eu-01
          aws-region: eu-west-1

  deploy-prod:
    if: github.ref == 'refs/heads/prod'
    needs:
      - build
    runs-on: ubuntu-24.04
    concurrency: tr-dss-slopes-data-deploy-prod
    environment: prod
    env:
      PROJECT_NAME: tr-dss-slopes
      VALUES_YAML: infrastructure/helm/slopes-data.prod.yaml
    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Customize values.yaml
        working-directory: ${{ env.WORKING_DIRECTORY }}
        run: |
          yq -i '
            .image.repository="${{ env.DOCKER_IMAGE }}" |
            .image.tag="${{ env.DOCKER_TAG }}" |
            .image.pullPolicy="IfNotPresent"
          ' ${{ env.VALUES_YAML }}

          yq -i '.envSecret.ODH_CORE_TOKEN_CLIENT_ID = "${{ secrets.CONTENT_DSS_CLIENT_ID_PROD }}"' ${{ env.VALUES_YAML }}
          yq -i '.envSecret.ODH_CORE_TOKEN_CLIENT_SECRET = "${{ secrets.CONTENT_DSS_CLIENT_SECRET_PROD }}"' ${{ env.VALUES_YAML }}

      - name: Deploy on cluster  
        uses: noi-techpark/github-actions/helm-deploy@v2
        with:
          k8s-name: ${{ env.PROJECT_NAME }}
          k8s-namespace: collector
          chart-path: helm/generic-collector
          values-file: ${{ env.WORKING_DIRECTORY }}/${{ env.VALUES_YAML }}
          aws-access-key-id: ${{ secrets[vars.AWS_KEY_ID] }}
          aws-secret-access-key: ${{ secrets[vars.AWS_KEY_SECRET] }}
          aws-eks-cluster-name: aws-main-eu-01
          aws-region: eu-west-1
