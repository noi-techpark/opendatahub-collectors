name: CI/CD dc-api-crawler-parking-affluences

on:
  push:
    paths:
      - "collectors/api-crawler/**"
      - "!collectors/api-crawler/infrastructure/helm/*"
      - "!collectors/api-crawler/infrastructure/crawler-config/*"
      - "collectors/api-crawler/infrastructure/helm/parking-affluences.yaml"
      - "collectors/api-crawler/infrastructure/crawler-config/parking-affluences.silky.yaml"
      - ".github/workflows/dc-api-crawler-parking-affluences.yml"

env:
  WORKING_DIRECTORY: collectors/api-crawler
  HTTP_CONFIG_PATH: infrastructure/crawler-config/parking-affluences.silky.yaml
  DOCKER_IMAGE: ghcr.io/noi-techpark/opendatahub-collectors/dc-api-crawler
  DOCKER_TAG: ${{ github.sha }}
  KUBERNETES_NAMESPACE: collector
  VALUES_YAML: infrastructure/helm/parking-affluences.yaml
  K8S_NAME: dc-api-crawler-parking-affluences

jobs:
  build:
    runs-on: ubuntu-24.04
    concurrency: dc-api-crawler-parking-affluences-build
    steps:
    - name: Checkout source code
      uses: actions/checkout@v4

    - name: Build and push images
      uses: noi-techpark/github-actions/docker-build-and-push@v2
      with:
        working-directory: ${{ env.WORKING_DIRECTORY }}/infrastructure
        docker-username: ${{ github.actor }}
        docker-password: ${{ secrets.GITHUB_TOKEN }}

  deploy-parking-affluences-test:
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/feat/parking-affluences'
    needs: build
    runs-on: ubuntu-24.04
    concurrency: dc-api-crawler-parking-affluences-test
    environment: test
    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Customize values.yaml
        working-directory: ${{ env.WORKING_DIRECTORY }}
        run: |
          yq -i '.image.repository="${{ env.DOCKER_IMAGE }}"' ${{ env.VALUES_YAML}}
          yq -i '.image.tag="${{ env.DOCKER_TAG }}"' ${{ env.VALUES_YAML }}
          yq -i '.image.pullPolicy="IfNotPresent"' ${{ env.VALUES_YAML }}
          yq -i '.envSecret.AFFLUENCES_API_KEY = "${{ secrets.AFFLUENCES_API_KEY }}"' ${{ env.VALUES_YAML }}
          yq -i '.configMap.files["config.yaml"] = load_str("${{ env.HTTP_CONFIG_PATH }}")' ${{ env.VALUES_YAML }}

      - name: Deploy on cluster
        uses: noi-techpark/github-actions/helm-deploy@v2
        with:
          k8s-name: dc-api-crawler-parking-affluences
          k8s-namespace: collector
          chart-path: helm/generic-collector
          values-file: ${{ env.WORKING_DIRECTORY}}/${{ env.VALUES_YAML }}
          aws-access-key-id: ${{ secrets[vars.AWS_KEY_ID] }}
          aws-secret-access-key: ${{ secrets[vars.AWS_KEY_SECRET] }}
          aws-eks-cluster-name: aws-main-eu-01
          aws-region: eu-west-1

  # deploy-parking-affluences-prod:
  #   if: github.ref == 'refs/heads/prod'
  #   needs: build
  #   runs-on: ubuntu-24.04
  #   concurrency: dc-api-crawler-parking-affluences-prod
  #   environment: prod
  #   steps:
  #     - name: Checkout source code
  #       uses: actions/checkout@v4

  #     - name: Customize values.yaml
  #       working-directory: ${{ env.WORKING_DIRECTORY }}
  #       run: |
  #         yq -i '.image.repository="${{ env.DOCKER_IMAGE }}"' ${{ env.VALUES_YAML}}
  #         yq -i '.image.tag="${{ env.DOCKER_TAG }}"' ${{ env.VALUES_YAML }}
  #         yq -i '.image.pullPolicy="IfNotPresent"' ${{ env.VALUES_YAML }}
  #         yq -i '.envSecret.AFFLUENCES_API_KEY = "${{ secrets.AFFLUENCES_API_KEY }}"' ${{ env.VALUES_YAML }}
  #         yq -i '.configMap.files["config.yaml"] = load_str("${{ env.HTTP_CONFIG_PATH }}")' ${{ env.VALUES_YAML }}

  #     - name: Deploy on cluster
  #       uses: noi-techpark/github-actions/helm-deploy@v2
  #       with:
  #         k8s-name: dc-api-crawler-parking-affluences
  #         k8s-namespace: collector
  #         chart-path: helm/generic-collector
  #         values-file: ${{ env.WORKING_DIRECTORY}}/${{ env.VALUES_YAML }}
  #         aws-access-key-id: ${{ secrets[vars.AWS_KEY_ID] }}
  #         aws-secret-access-key: ${{ secrets[vars.AWS_KEY_SECRET] }}
  #         aws-eks-cluster-name: aws-main-eu-01
  #         aws-region: eu-west-1
