rootContext: {}
stream: false

steps:
  # ── Webcams ─────────────────────────────────────────────────────────────────
  - type: request
    name: "Fetch DSS Webcams"
    request:
      url: "https://www.dolomitisuperski.com/jsonexport/export/webcams?username=${DSS_USERNAME}&password=${DSS_PASSWORD}"
      method: GET
    resultTransformer: "."
    mergeOn: .dssWebcams = $res
